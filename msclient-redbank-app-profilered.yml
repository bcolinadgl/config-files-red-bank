hystrixRedBank:
  isolationProperties:
    isolationStrategyKey: "execution.isolation.strategy"
    isolationStrategyValue: "SEMAPHORE"
  IsolationThreadProperites:
    isolationThreadKey: "execution.isolation.thread.timeoutInMilliseconds"
    isolationThreadValue: "3000"
